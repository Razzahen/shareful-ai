---
title: Configuration
description: Configure the shareful-ai CLI with user settings and environment variables.
---

The CLI uses a combination of user-level configuration and environment variables.

## User configuration

The CLI stores user settings at:

```
~/.shareful/config.json
```

### Configuration fields

| Field | Type | Description |
| --- | --- | --- |
| `sharesRepo` | string | Default path to your shares repository |

### Example

```json
{
  "sharesRepo": "/Users/you/code/my-shares"
}
```

When `sharesRepo` is set, the CLI uses it as the default repository location. Otherwise, it uses the current working directory.

## Environment variables

| Variable | Description |
| --- | --- |
| `DISABLE_TELEMETRY` | Set to `1` to disable anonymous telemetry |
| `DO_NOT_TRACK` | Set to `1` to disable anonymous telemetry (alternative) |

### Disabling telemetry

The CLI collects anonymous telemetry by default to help improve the platform. To opt out, set either variable in your shell profile:

```bash
# Add to ~/.bashrc, ~/.zshrc, or equivalent
export DISABLE_TELEMETRY=1
```

Or set it for a single command:

```bash
DISABLE_TELEMETRY=1 npx shareful-ai create
```

### What telemetry collects

| Event | Data |
| --- | --- |
| `share` | Share slug, share count |
| `init-repo` | Repository name |
| `check` | Valid count, error count |
| `add-skills` | None |

No personal information, IP addresses, or device identifiers are collected.

## Directory structure

A shares repository follows this structure:

```
my-shares/
  shares/
    fix-nextjs-hydration/
      SHARE.md
    prisma-pooling/
      SHARE.md
```

The `shares/` directory name and the `SHARE.md` filename are constants defined by the CLI and cannot be changed.

## Requirements

- **Node.js** 18 or later
- **Git** for version control
- **GitHub** repository (for indexing and discovery)

## Next steps

- Read the [CLI reference](/docs/cli) for all commands
- Learn how to [create shares](/docs/creating-shares)
- See [finding shares](/docs/finding-shares) for search options
